services:
  - type: web
    name: stream-hub-pro
    env: node
    buildCommand: pnpm install && pnpm build
    startCommand: node dist/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.x
