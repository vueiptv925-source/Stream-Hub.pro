# Notes

- Direct fetch to vidsrc.to works (returns HTML with iframe to vsembed.ru)
- AllOrigins proxy returns empty - may be blocked or rate-limited
- The provider uses AllOrigins proxy for CORS bypass
- Need to test without proxy (direct fetch) since we're on a server
